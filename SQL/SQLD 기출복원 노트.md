- 개체 무결성의 특징
    null이면 안됨
    중복 안됨

- 1차 정규화 - 도메인 원자성을 확보
- Barker 표기방식은 nullable을 확인할 수 있다
- 뷰를 통해 DML 작업은 단일 테이블일때만 가능하고, 복합테이블일때는 제약이 생긴다
- ROLLUP은 계층적 집계를 출력하고, CUBE는 모든 조합을 출력한다
- GROUPING SETS는 원하는 집계를 나열할 수 있다
- NOT IN (NULL)일 경우 공집합이 출력된다

- DENSE_RANK는 동순위를 건너뛰지 않는다